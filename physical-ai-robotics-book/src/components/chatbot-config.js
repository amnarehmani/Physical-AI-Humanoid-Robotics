const CONFIG = {
  BACKEND_URL: "http://127.0.0.1:8000/api/v1", // no trailing slash
  ENDPOINTS: {
    QUERY: "/query/",               // note trailing slash
    QUERY_SELECTION: "/query/selection/"
  }
};

export default CONFIG;
